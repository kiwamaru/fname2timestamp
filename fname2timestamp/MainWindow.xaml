<Window x:Class="fname2timestamp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:fname2timestamp"
        xmlns:behaviors="clr-namespace:fname2timestamp.Behaviors"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:behaviors1="clr-namespace:fname2timestamp.Behaviors;assembly=BindSelectedItemsBehavior"
        Title="fname2timestamp" Height="458" Width="1002"
        prism:ViewModelLocator.AutoWireViewModel="True"
        AllowDrop="True"
        Drop="Window_Drop" PreviewDragOver="Window_PreviewDragOver" Icon="Resource/Calender.ico"
        Closing="Window_Closing">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Height="30" Orientation="Horizontal" FlowDirection="RightToLeft">
            <Button x:Name="btnAllDel" Width="120" 
                    Command="{Binding RemoveAllFileCommand}" IsEnabled="{Binding HasFile}">
            <StackPanel Orientation="Horizontal">
                    <Label Content="全リスト削除"></Label>
                <Image Width="24" Height="24" HorizontalAlignment="Left" Source="Resource/Trashbox.ico"/>
                </StackPanel>
            </Button>
            <Button x:Name="btnSelDel" Width="150" IsEnabled="{Binding CanDeleteFile}"
                    Command="{Binding RemoveFileCommand}">
                <StackPanel Orientation="Horizontal">
                    <Label Content="リストから選択削除"></Label>
                    <Image Source="Resource/Trashbox.ico" Width="24" Height="24" HorizontalAlignment="Left"></Image>
                </StackPanel>
            </Button>

            <Button x:Name="btnAllExec" Width="100" 
                    Command="{Binding ChangeAllTimestampCommand}" IsEnabled="{Binding CanChangeAllTimestamp}">
                <StackPanel Orientation="Horizontal">
                    <Label Content="全実行"></Label>
                    <Image Source="Resource/cal_all.ico" Width="24" Height="24" HorizontalAlignment="Left"></Image>
                </StackPanel>
            </Button>

            <Button x:Name="btnSelExec" Width="100" IsEnabled="{Binding CanChangeTimestamp}"
                    Command="{Binding ChangeTimestampCommand}">
                <StackPanel Orientation="Horizontal">
                    <Label Content="選択実行" ></Label>
                    <Image Source="Resource/Calender.ico" Width="24" Height="24" HorizontalAlignment="Left"></Image>
                </StackPanel>
            </Button>
            <CheckBox x:Name="updateTimeStampChcekBox" Content="更新日時" VerticalAlignment="Center" Margin="30,0,0,0" IsChecked="{Binding UpdateDate}" Click="updateChcekBox_Checked"
                      Command="{Binding UpdateFileCommand}"/>
            <CheckBox x:Name="creationTimeStampChcekBox" Content="作成日時" Margin="30,0,0,0" VerticalAlignment="Center" Padding="0" IsChecked="{Binding CreationDate}" Click="creationDateChcekBox_Checked"
                      Command="{Binding UpdateFileCommand}"/>
            <TextBlock VerticalAlignment="Center">:タイムスタンプ変更対象</TextBlock>
        </StackPanel>
        <TextBlock DockPanel.Dock="Bottom" Text="{Binding StatusBarMessage}"/>
        <DataGrid ItemsSource="{Binding FileList}"
                   AutoGenerateColumns="False" Margin="0,0,0,0" CanUserAddRows="False">
            <DataGrid.Resources>
                <local:MyBoolConverter x:Key="MyBoolConverter"/>
                <local:MyDateTimeConverter x:Key="MyDateTimeConverter"/>
            </DataGrid.Resources>
            <DataGrid.ItemContainerStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding isValid}" Value="false">
                            <Setter Property="Background" Value="Gray" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding success}" Value="true">
                            <Setter Property="Background" Value="LightGreen" />
                        </DataTrigger>
                    </Style.Triggers>
                    
                </Style>
            </DataGrid.ItemContainerStyle>
        <DataGrid.Columns>
            <!--<DataGridTextColumn Header="No" Binding="{Binding num}"/>-->
            <DataGridTextColumn Header="ファイル名" Binding="{Binding name}" Width="200" MinWidth="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="ファイルの種類" Binding="{Binding ext}" Width="100" MinWidth="20" IsReadOnly="True"/>
                <DataGridTextColumn Header="フルパス" Binding="{Binding path}" Width="60" IsReadOnly="True"/>
                <DataGridTextColumn Header="現在の作成日時" Binding="{Binding create_dtime, StringFormat=yyyy/MM/dd-HH:mm:ss}" IsReadOnly="True"/>
                <DataGridTextColumn Header="現在の更新日時" Binding="{Binding update_dtime, StringFormat=yyyy/MM/dd-HH:mm:ss}" IsReadOnly="True"/>
                <DataGridTextColumn Header="変更実行後" Binding="{Binding f2t_dtime, Converter={StaticResource MyDateTimeConverter}, StringFormat=yyyy/MM/dd-HH:mm:ss}" IsReadOnly="True"/>
                <!--<DataGridTextColumn Header="変換可否" Binding="{Binding isValid, Converter={StaticResource MyBoolConverter}}"/>-->
                <!--<DataGridTextColumn Header="現在のアクセス日時" Binding="{Binding access_dtime, StringFormat=yyyy/MM/dd-HH:mm:ss}"/>-->
                <DataGridTextColumn Header="ファイルサイズ" Binding="{Binding size, StringFormat=\{0:N0\} bytes}" IsReadOnly="True"/>
                <DataGridTextColumn Header="備考" Binding="{Binding err_message}" IsReadOnly="True"/>
            </DataGrid.Columns>
            <i:Interaction.Behaviors>
                <behaviors:DataGridSelectedItemsBlendBehavior SelectedItems="{Binding ListSelectedItem}"/>
            </i:Interaction.Behaviors>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectedCellsChanged">
                    <i:InvokeCommandAction Command="{Binding SelectedCellsChangedCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
    </DataGrid>
    </DockPanel>
</Window>
